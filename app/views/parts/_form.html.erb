<%= form_with(model: part, local: true) do |form| %>
  <% if part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part.errors.count, "error") %> prohibited this part from being saved:</h2>

      <ul>
      <% part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :in %>
    <%= form.text_field :in %>
  </div>

  <div class="field">
    <%= form.label :out %>
    <%= form.text_field :out %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="field">
    <%= form.label :viewing_form_id %>
    <%= form.text_field :viewing_form_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
